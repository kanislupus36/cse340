<% if (title) { %>
    <h1>
        <%= title %>
    </h1>
    <% } else { res.redirect('/') } %>
        <%- messages() %>
            <% if (errors) { %>
                <ul class="notice">
                    <% errors.array().forEach(error=> { %>
                        <li>
                            <%= error.msg %>
                        </li>
                        <% }) %>
                </ul>
                <% } %>
                <% if (locals.accountData) { %>
                    <p><a href="/account/account-update/<%= locals.accountData.account_id %>">update
                            account
                            information</a></p>
                    <% if (locals.accountData.account_type=='Client' ) { %>
                        <h2>Welcome, <%= locals.accountData.account_firstname%>
                        </h2>
                        <% } else { %>
                            <h2>Welcome, <%= locals.accountData.account_firstname%>
                            </h2>
                            <h3>Inventory Management</h3>
                            <p><a href="/inv/management">Manage Inventory</a></p>
                    
                                    <% } %>
                                        <% } %>